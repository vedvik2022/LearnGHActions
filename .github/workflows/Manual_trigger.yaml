name: Manual_run_workflow
on: 
  workflow_dispatch:
jobs:
  second-job:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "This is manual triggered workflow!"
      - name: Step 2
        run: echo "for manual workflow testing"
